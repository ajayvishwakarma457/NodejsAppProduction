{
  "name": "sst-serverless-deployment",
  "version": "1.0.0",
  "description": "SST (Serverless Stack) infrastructure configurations for AWS",
  "scripts": {
    "dev": "sst dev",
    "deploy": "sst deploy",
    "remove": "sst remove"
  },
  "dependencies": {
    "sst": "^2.0.0"
  },
  "devDependencies": {
    "aws-cdk-lib": "^2.0.0",
    "constructs": "^10.0.0"
  }
}
