apiVersion: apps/v1
kind: Deployment
metadata:
  name: nodejs-monolith-canary
  namespace: default
  labels:
    app: nodejs-monolith
    track: canary
spec:
  replicas: 1 # Starts with 1 replica for testing
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 25%
      maxUnavailable: 25%
  selector:
    matchLabels:
      app: nodejs-monolith
      track: canary
  template:
    metadata:
      labels:
        app: nodejs-monolith
        track: canary
    spec:
      containers:
        - name: monolith
          image: nodejs-monolith-app:v1.1.0-canary # Points to new canary image
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 5000
              name: http
          resources:
            requests:
              cpu: 100m
              memory: 256Mi
            limits:
              cpu: 500m
              memory: 512Mi
          envFrom:
            - configMapRef:
                name: nodejs-app-config
            - secretRef:
                name: nodejs-app-secrets
          env:
            - name: PORT
              value: "5000"
          livenessProbe:
            httpGet:
              path: /health
              port: 5000
            initialDelaySeconds: 15
            periodSeconds: 20
            timeoutSeconds: 5
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /health
              port: 5000
            initialDelaySeconds: 10
            periodSeconds: 10
            timeoutSeconds: 3
            failureThreshold: 3
